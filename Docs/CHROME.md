# chrome.sh

Installs Google Chrome on Fedora.

## Steps

1. **Add repository** — Registers the official Google Chrome stable repo using `dnf config-manager`.
2. **Install package** — Installs the `google-chrome-stable` package via `dnf`.
